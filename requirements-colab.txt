# Additional dependencies used by the experiment runner.
# PyTorch is intentionally omitted because Colab and many GPU clusters provide
# environment-specific CUDA builds.
numpy>=1.24
tqdm>=4.66
optuna>=3.6
